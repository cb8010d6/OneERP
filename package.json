{
  "name": "one-erp",
  "description": "OneERP 是面向制造与供应链企业的 AI Native ERP，聚焦真实库存、财务可靠性、权限治理和单机可恢复生产部署。",
  "author": "OneERP Team",
  "private": true,
  "license": "MIT",
  "keywords": [
    "erp",
    "manufacturing",
    "inventory",
    "finance",
    "ai-native",
    "nestjs",
    "nextjs",
    "prisma"
  ],
  "scripts": {
    "seed": "npm --prefix apps/api run seed",
    "prepare": "husky",
    "commitlint": "commitlint --config commitlint.config.cjs --from=HEAD~1 --to=HEAD",
    "typecheck": "npm --prefix apps/api run typecheck && npm --prefix apps/web run typecheck",
    "lint": "npm --prefix apps/api run lint && npm --prefix apps/web run lint",
    "test": "npm --prefix apps/api run test -- --passWithNoTests && npm --prefix apps/web run test",
    "build": "npm --prefix apps/api run build && npm --prefix apps/web run build",
    "validate": "npm --prefix apps/api run prisma:validate && npm --prefix apps/api run prisma:generate && npm run typecheck && npm run lint && npm run test && npm run build",
    "quickstart": "powershell -ExecutionPolicy Bypass -File scripts/quickstart.ps1",
    "backup": "powershell -ExecutionPolicy Bypass -File scripts/backup.ps1",
    "deploy:check": "powershell -ExecutionPolicy Bypass -File scripts/deploy-check.ps1",
    "prod:smoke": "powershell -ExecutionPolicy Bypass -File scripts/prod-smoke.ps1",
    "business:acceptance": "powershell -ExecutionPolicy Bypass -File scripts/business-acceptance.ps1",
    "staff:smoke": "powershell -ExecutionPolicy Bypass -File scripts/staff-permission-smoke.ps1",
    "prod:audit": "powershell -ExecutionPolicy Bypass -File scripts/audit-prod-config.ps1",
    "backup:schedule": "powershell -ExecutionPolicy Bypass -File scripts/install-backup-schedule.ps1",
    "restore:drill": "powershell -ExecutionPolicy Bypass -File scripts/restore-drill.ps1"
  },
  "devDependencies": {
    "@commitlint/cli": "^19.8.1",
    "@commitlint/config-conventional": "^19.8.1",
    "fs-extra": "^11.3.4",
    "husky": "^9.1.7"
  },
  "overrides": {
    "fast-uri": "3.1.3",
    "js-yaml": "4.3.0"
  }
}
